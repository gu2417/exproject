# favorites.txt

클라이언트 로컬 즐겨찾기 저장 파일이다.

## 형식

```text
<type>|<id>|<name>
```

| 필드 | 설명 |
|------|------|
| `type` | `stop`, `bus`, `station` |
| `id` | 정류장 ID, 버스 번호, 역 ID |
| `name` | 화면 표시명 |

## 규칙

- 중복 기준은 `type` + `id`다.
- 파일이 없으면 빈 즐겨찾기로 처리하고 필요 시 생성한다.
- 손상된 행은 건너뛴다.

